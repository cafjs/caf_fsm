
/**
 * @global
 * @typedef {Object | Array | string | number | null | boolean} jsonType
 *
 */

/**
 * @global
 * @typedef {Object} fsmDescType
 * @property {Object} config See `createMachine()` config in
 * https://xstate.js.org/docs/packages/xstate-fsm/#api
 * @property {Object=} options See `createMachine()` options, with property
 * `actions` a lookup object for string actions.
 * @property {function=} stateListener An optional listener to state changes,
 * of type (state:string) => void
 */
